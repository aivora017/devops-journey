##python

it has mainly 2 verions
python2  (2000 - 2010)
python3 (2008 to present)

# install pyhton

.yum install python2
.yum install python3


# pip - python package manager

commands
. pip install package  -> to install a python package
. pip show package -> to show a package detail
. pip2 -c "import sys; print(sys.path)" ->  ot prints all the paths that pip search when a package is called
.pip install -r requirements -> it is used to install all the requirements for a program from a requirement file
.pip install package --upgrade -> it is used to upgrade an installed package
.pip uninstall package -> it is used to uninstall a package


some other package managers of python are

.easy-install
it uses .egg file it works like jar in java for python
 
. wheels

pip install app.whl


